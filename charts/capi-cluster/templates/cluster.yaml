apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: {{ include "capi-cluster.metadata.name" . }}
  namespace: {{ include "capi-cluster.metadata.namespace" . }}
  labels: {{- include "capi-cluster.metadata.labels" . | nindent 4 }}
spec:
  controlPlaneRef:
    apiVersion: {{ .Values.controlPlaneRef.apiVersion }}
    kind: {{ .Values.controlPlaneRef.kind }}
    name: {{ .Values.controlPlaneRef.name | default (include "capi-cluster.metadata.name" .) }}
    namespace: {{ .Values.controlPlaneRef.namespace | default (include "capi-cluster.metadata.namespace" .) }}
  infrastructureRef:
    apiVersion: {{ .Values.infrastructureRef.apiVersion }}
    kind: {{ .Values.infrastructureRef.kind }}
    name: {{ .Values.infrastructureRef.name | default (include "capi-cluster.metadata.name" .) }}
    namespace: {{ .Values.infrastructureRef.namespace | default (include "capi-cluster.metadata.namespace" .) }}
